# 任务书目录（docs/tasks/）

本目录只放**未开始**阶段的任务书（`P<n>-<名>.md`）。

- 已完成的任务书已归档到 `../archive/tasks/`（P1 / P2 / P3 / P3_6 / P3_7 / P3_8 / P4-R3）。
- 新任务书必须**自包含**（subagent 看不到历史对话）：必读清单 → 字段级规格 → 验收判据
  （逐条可判定 + 一句证据）。
- 写作风格参考归档里的 `../archive/tasks/P3_8-效果系统.md`。
- 每份任务书完成后：把验收数字写进 `../实现路线图.md` 对应阶段条目，然后 `git mv` 到 `../archive/tasks/`。

当前待写：**P4 突破考验**（见 `../实现路线图.md`）。
