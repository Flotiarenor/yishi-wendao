# 文档地图（docs/）

> 目的：一页看清**每份文档管什么、谁说了算、还算不算数**。
>
> ## ⚠️ 读文档前必读（2026-09-10 统一口径）
>
> 1. **"现状是什么样"只有一个事实源：根 `README.md` 的「当前状态」事实表。**
>    测试项数 / smoke 指纹 / 世界指纹 / 栅格 / 视野 / 性能等数字**只在那一处维护**。
> 2. `docs/` 下所有文档里的数字（测试项数、指纹、格宽、视野半径、性能）一律是**当时快照**，
>    **不作为现状依据**。它们记录的是"当时做了什么、为什么"，价值在理由而不在数字。
> 3. **发现文档与代码冲突 → 以代码 + 根 README 事实表为准**，改文档前先跑一次基线
>    （`.venv\Scripts\python.exe -X utf8 main.py test`）。
> 4. **新增文档时不要再复制数字**——写"见根 README 事实表"。
>    数字被复制的次数 = 将来被误导的次数（本次统一口径就是因为这个）。
> 5. 新会话入口顺序：根 `README.md`「当前状态」→ 本文件 → `设计定案.md`（规则）→
>    `实现路线图.md`（进度与顺序）→ 当前阶段任务书。

## 一、权威文档（改设计 = 改这里）

| 文档 | 管什么 | 权威性 | 状态 |
|---|---|---|---|
| `设计定案.md` | 玩法规则、世界规则、境界/物品/突破/势力设计 | **规则唯一权威** | 活跃 |
| `战斗系统定案.md` | 战斗：时间轴、双约束、动作模型、五段乘区、效果时间窗口 | **战斗唯一权威**；取代 `技能与效果架构.md` §四 | 活跃 |
| `地图与场所定案.md` | 地图：连续坐标、时间刻度（息/时辰/日）、代价场寻路、迷雾、城镇场所 | **地图与时间唯一权威**；修订 `设计定案.md` §九 | 活跃（2026-09-10 定案；T1/T2 已实现，T3–T6 未实现） |
| `实现路线图.md` | 阶段顺序、每阶段产出与验收、依赖关系 | **实现顺序唯一依据** | 活跃 |
| `Web架构方案.md` | 壳层：FastAPI + Vue3 +（未来）pywebview、API 契约 | 壳层唯一依据 | P3.5–P3.7 已落地；pywebview 未做 |
| `设计-结算与转世.md` | P6 结算与转世思路稿（**未实现**） | 思路稿，非规则权威 | 活跃（P6 前的讨论底稿；`实现路线图.md` P6 已引用，**保留**） |
| `提交规范.md` | 提交信息格式（Conventional Commits + 中文）、分支策略、作者身份、本机 git 备忘 | 仅约束**提交信息**，与玩法设计无关 | 活跃 |

## 二、当前阶段任务书（`tasks/`）

| 文档 | 说明 | 状态 |
|---|---|---|
| `tasks/P4-地图与时间地基.md` | P4 总任务书（T1…T6 规格 + 验收判据） | 活跃（T1/T2/T3 已完成并勾选；**T4 起仍有效**） |
| `tasks/P4-T2-世界地图内核.md` | T2 任务书（重构版）；**§3.3 代价场窗口化已收口，规格作废**（见文首告示） | 活跃但**部分作废** |
| `tasks/P4-T3-移动.md` | T3 任务书：**已完成**；§0.2 记录"`safe` 档兑现不了更安全"的实测与处置 | 已完成（留档） |
| `tasks/README.md` | 任务书目录约定 | 活跃 |

> **内容层的城镇布局**（工具产物）已落盘：`content/town_layouts/outer-青石镇.json` ——
> 用户用 `tools/town_editor.html` 画好、经 `node tools/run_town_diagnose.js` 复核 **0 硬伤** 的成品；
> 载体代码在 `content/town_layouts.py`（`TownLayout` + `layouts_for_tier()`，**自动剔除体检有硬伤的模板**）。

> 未开始阶段的任务书写在 `tasks/`；**已完成的阶段任务书 `git mv` 到 `archive/tasks/`**。

## 三、会话记录（历史快照，数字不作现状用）

| 文档 | 说明 | 状态 |
|---|---|---|
| `会话结算-2026-09-11-续.md` | **最新一次会话的结算**（接着下面那份）：复核上一份交接的每条主张（全属实，但**事实表自己失准**）+ 修 E2E 可信度 + 删死代码 + **查出并修掉地图坐标朝向 bug**（底图南在上/前端北在上 → 上下镜像；含"为什么中心点测不出来"的排查过程）+ y 轴朝向回归护栏 | **最新；地图朝向的来龙去脉看这份** |
| `会话结算-2026-09-11.md` | 该次对话的结算：议题流水 / 用户拍板 / **设计思路**（舆图为何必须"低精度"、LOD 与探索边界、音频为何归宿主进程、桌面壳的身份四件事）/ 误区与教训（傅里叶短折线、预热净亏损、E2E 固定 sleep）/ 产出 / 后续 | **有效；舆图与桌面壳的来龙去脉看这份** |
| `交接-2026-09-11-P4T3复核.md` | **最新一份项目交接（先读这份）**：对上一份交接的**逐条复核**（基线实跑通过、主张属实）+ 补出四处代码事实（`discovered` 只读不写 / `visible_points` 是死代码 / `map_view` 缓存键含 `len(disc)` 会过期 / 舆图档语义与定案 §5 有分歧）+ T4 最小可玩切法（A/B/C）与三个待拍板问题 | **最新；T4 开工前必读** |
| `交接-2026-09-11-P4T3.md` | 上一份项目交接：P4-T3 移动完成 + 城镇设计工具落地；含设计思路（代价域模型/性能三坑/内容点分布/两层设计/判据同口径）、后续、未实现清单、技术债 | **历史快照（但设计理由与"未实现"清单仍有效）** |
| `审查-town_editor-v3.md` | 城镇标注工具全文审查：三条必修 bug、叠加视图重做的量化论证、CPU 起飞的实测与改法、三轮实测记录 | **判据踩坑的权威记录**（改工具前必读） |
| `交接-2026-09-10-P4T2.md` | 上一份交接：环境/状态/设计理由/后续/未实现设计与技术债 | 历史快照 |
| `会话结算-2026-09-10.md` | 该次对话的结算：议题流水/拍板决策/犯错与纠正/实测数据/产出/下一步 | 历史快照 + §六/§七（T2-R3 收口结论） |
| `会话交接.md` | 工作方式、历史决策、已知取舍、未决问题；**含大量 P3.x 时代数字** | 偏历史；未决问题仍有效 |

## 四、参考 / 部分作废（别当现状读）

| 文档 | 说明 | 状态 |
|---|---|---|
| `数据格式草案.md` | P9 生成器目标 JSON 格式参考；与当前 `content/*.py` dataclass 实现**不同** | 草案（P9 用） |
| `设定-灵力系统底层架构.md`、`设定-世界规则与修炼境界.md` | 世界观素材（灵力五层、境界描述）——**不进引擎**，仅作叙事/生成器参考 | 素材层 |

> 当前**状态行为**的权威是 `engine/status.py`（代码即文档）；**效果展示模板**是 `content/effects.py`。

## 五、归档

| 目录 | 内容 |
|---|---|
| `archive/tasks/` | 已完成阶段的任务书（P1 / P2 / P3 / P3_6 / P3_7 / P3_8 / P4-R3 / **P4-T2 首版**），留档不再维护 |
| `archive/技能与效果架构.md` | P3.8 效果系统定案；**已被 `战斗系统定案.md` + `engine/status.py` 完全取代**（2026-09-10 归档） |
| `archive/早期AI网页规格书-已废弃.md` | 早期 AI 网页形态，已废弃 |

## 六、已知文档债（待清）

1. `会话交接.md` 体量大（~30 KB）且含大量 P3.x 历史数字，建议后续拆成
   「当前状态 / 已知取舍 / 未决问题」三份（当前状态部分已由根 README 事实表承担）。
2. `Web架构方案.md` 引用的 `docs/tasks/P3_5-动作结果结构化.md` 从未落盘（P3.5 由主会话直接实现）。
3. **前端地图与文档口径**：`frontend/src/views/MapScreen.vue` 现仍是 P3.7 时代的
   **地点卡片列表**（读 `frontend/src/types/sites.ts` 的 `SITES`，无 canvas、无坐标、不读 worldmap），
   尚未接世界地图内核——T6 才改。`TownScreen.vue` **不存在**（T5/T6 才新建）。
